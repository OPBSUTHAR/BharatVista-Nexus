{
  "$schema": "https://opencode.ai/schema.json",
  "instructions": ["AGENTS.md"],
  "permission": {
    "allow": ["read", "edit", "write", "bash"],
    "deny": ["bash:curl *SLDC*", "bash:curl *CEA*", "bash:*scada*"]
  },
  "tools": {
    "note": "All ingestion must be via authorized api.data.gov.in endpoint with DATA_GOV_IN_API_KEY, rate-limited, cached to SQLite. Do not add scrapers for live critical-infrastructure telemetry."
  },
  "memory": {
    "persist": ["AGENTS.md"],
    "goal": "BharatVista Nexus (codename project_C_osint) — Pure-C high-performance open-data nexus for India & world, only allowlisted public APIs (data.gov.in), SQLite cache, Leaflet/OSM, VS Code cross-platform."
  }
}
