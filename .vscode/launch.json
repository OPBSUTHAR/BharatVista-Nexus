{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run C Server",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/server",
      "args": ["8080"],
      "cwd": "${workspaceFolder}",
      "MIMode": "gdb",
      "preLaunchTask": "Build Server"
    }
  ]
}
